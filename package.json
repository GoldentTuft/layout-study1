{
  "devDependencies": {
    "cssnano": "^4.1.10",
    "elm-hot": "^1.1.4",
    "node-elm-compiler": "^5.0.4",
    "sass": "^1.26.3"
  },
  "dependencies": {
    "elm-test": "^0.19.1-revision2",
    "style.css": "^1.0.0"
  },
  "scripts": {
    "clean": "del .\\dist\\*.*",
    "dev:remote": "npm run clean && parcel src/index.html --public-url ./",
    "dev": "parcel src/index.html",
    "build": "npm run clean && parcel build src/index.html --public-url ./",
    "test": "elm-test"
  }
}
